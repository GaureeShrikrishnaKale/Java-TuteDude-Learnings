package test;

public class MethodOverriding {
	//We cannot play game on smart watch, so should not be able to call the method from smartphone for playing game
	
	//1. Method Overriding 

	//Define the same method in SmartWatch and there write cannot play
	
	//control first will check in same class if method found that we written that will be executed and if not found the parent class method will get executed.
	
	
	//2. Interfaces
	//A class ( piece of code )  that will have methods signatures no body
	//using an interface is like signing a contract ( I will write all the methods that are given in the interface
}
